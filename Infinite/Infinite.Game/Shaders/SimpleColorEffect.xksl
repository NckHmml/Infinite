shader SimpleColorEffect : ShadingBase
{
    stage stream float4 Color : COLOR;

    stage override float4 Shading() 
    {
        float4 normalShade = base.Shading();
        return lerp(normalShade, streams.Color, 0.9);
    }
};